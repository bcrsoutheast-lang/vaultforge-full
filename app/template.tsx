
import VaultForgeGlobalMemberNavGate from "./components/VaultForgeGlobalMemberNavGate";

export default function Template({ children }: { children: React.ReactNode }) {
  return <VaultForgeGlobalMemberNavGate>{children}</VaultForgeGlobalMemberNavGate>;
}
