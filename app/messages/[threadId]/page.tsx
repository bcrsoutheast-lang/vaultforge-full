import { redirect } from "next/navigation";
export const dynamic = "force-dynamic"; export const revalidate = 0;
function clean(v:unknown){return String(v||"").trim()} function fp(p:Record<string,string|string[]|undefined>,names:string[]){for(const n of names){const r=p[n];const v=Array.isArray(r)?r[0]:r;const t=clean(v);if(t)return t}return""} function safe(v:string){return clean(v).replace(/[^a-zA-Z0-9:_-]+/g,"-").replace(/^-+|-+$/g,"").slice(0,160)}
function source(p:Record<string,string|string[]|undefined>){const raw=fp(p,["source","type","context","folder","folder_key"]).toLowerCase();if(raw.includes("alert"))return"alert";if(raw.includes("pain"))return"pain";if(raw.includes("signal"))return"signal";if(raw.includes("routing")||raw.includes("route"))return"routing";if(raw.includes("intro"))return"introduction";if(raw.includes("project")||raw.includes("deal"))return"project";if(raw.includes("member"))return"member";return"general"}
export default function MessageThreadRedirectPage({params,searchParams}:{params:{threadId:string};searchParams?:Record<string,string|string[]|undefined>}){const sp=searchParams||{};const s=source(sp);const explicit=fp(sp,["thread_key","threadKey"]);const signal=fp(sp,["signal_id","signalId"]);const item=fp(sp,["item_id","itemId","deal_id","project_id","pain_id"]);const title=fp(sp,["title","subject"]);const identity=explicit||signal||item||decodeURIComponent(params.threadId||"")||"general";const key=explicit||`${s}:${safe(identity.replace(`${s}:`,""))||"general"}`;const next=new URLSearchParams();if(title)next.set("title",title);redirect(`/message-command/${encodeURIComponent(key)}${next.toString()?`?${next.toString()}`:""}`)}
